import { SalaryTypeDirective } from './salary-type.directive';

describe('SalaryTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new SalaryTypeDirective();
    expect(directive).toBeTruthy();
  });
});
